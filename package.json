{
  "name": "bun-mupdf-wasm-min-repro",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "compile": "bun build --target bun --production index.ts --compile --outfile test-build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "mupdf": "^1.3.3"
  }
}